tell me about yourself (this is from interviewer)
    - 

how does a week of my life as an MLOps engineer look like? (this is for students)

explain me your project(business/tool/technology)

    - handle the following
        - data ingestion pipelines
            - ETL services, projects and each of them have their own workflow
        - ML model training pipelines
        - ML model interence pipelines
        - /health of the application
            - availability 99%

            - not failing prod == app running with no problems == availablity at 99% == /health is good
            - BAU (business as usual, keep the lights on)

        - networking/firewall concepts (typically company focussed)
            AWS-VPC, all the work happens here (Virtual Private Cloud)
            - 196.192.400.1
            - 10.10.0.1
                10.10.0.2

mcp->repo->infra
    all erros are identified by LLM
    errors need code correction are done by the LLM
    PR is also raised by the LLM

you have to make a call to an LLM, how to make it?


semantics in programming

select * from table_name where col <100

landscape, what systems do we have?
    - checking by pabi
    